{
    "description": "Schema that describes a non-empty string array with non-empt strings",
    "type": "array", "minItems": 1, "uniqueItems": true,
    "items": { "type": "string", "minLength": 1 }            
}
