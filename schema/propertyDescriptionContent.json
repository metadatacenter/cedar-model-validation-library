{
  "description": "Schema that describes the schema of a property description in the _ui field in a template or element",
  "type": "object",
  "patternProperties": {
    "^[^.]+$": {
      "type": "string",
      "minLength": 1
    }
  },
  "additionalProperties": false
}