{
  "description": "Schema that describes a property description in a _ui field in a template or element",
  "type": "object",
  "patternProperties": {
    "^[^.]+$": {
      "type": "string"
    }
  },
  "additionalProperties": false
}