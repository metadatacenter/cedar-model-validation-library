{
    "description": "Schema that describes a 'properties' field in a template element",
    "type": "object",
    "properties": {
        "@type": { "$ref": "file:jsonLDTypeFieldContent.json" },
        "@id": {
            "description": "Meta-schema for the '@id' of an element occurrence, which an artifact may type either way: a stored template types it as a string, and one rendered since the server took over assigning it may also allow null, which is what a document carries for an occurrence that has no identity yet.",
            "anyOf": [
                { "$ref": "file:jsonLDIDFieldContent.json" },
                { "$ref": "file:jsonLDIDFieldContentWithNull.json" }
            ]
        },
        "@context": { "$ref": "file:templateElementContextFieldContent.json" }
    },
    "patternProperties": {
        "^(?!(@context|@type|@id|schema|xsd|pav|oslc|rdfs|\\$))[^\\.]+$": { "$ref": "file:userSpecifiedFields.json" }
    },
    "additionalProperties": false,
    "required": [ "@type", "@id", "@context" ]
}
