{
  "description": "Schema that describes the schema of a property label in the _ui field in a template or element",
  "type": "object",
  "patternProperties": {
    "^[^.]+$": {
      "type": "string",
      "minLength": 1
    }
  },
  "additionalProperties": false
}