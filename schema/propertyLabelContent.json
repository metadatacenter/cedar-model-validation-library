{
  "description": "Schema that describes a property label in a _ui field in a template or element",
  "type": "object",
  "patternProperties": {
    "^[^.]+$": {
      "type": "string",
      "minLength": 1
    }
  },
  "additionalProperties": false
}