{
    "description": "Schema that describes the schema of a 'schema:name' field in a 'properties' field in a template",
    "type": "object",
    "properties": {
        "type": { "type": "string", "enum": [ "string" ] },
        "minLength": { "type": "number", "enum": [ 1 ] }
    },
    "required": [ "type" ],
    "additionalProperties": false
}
