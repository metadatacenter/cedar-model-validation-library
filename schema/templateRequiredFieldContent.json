{
    "description": "Schema describing the schema of the 'required' field in a template",
    "type": "array", "minItems": 9, "uniqueItems": true,
    "items": [
        { "type": "string", "enum": [ "@context" ] },
        { "type": "string", "enum": [ "@id" ] },
        { "type": "string", "enum": [ "schema:isBasedOn" ] },
        { "type": "string", "enum": [ "schema:name" ] },
        { "type": "string", "enum": [ "schema:description" ] },
        { "type": "string", "enum": [ "pav:createdOn" ] },
        { "type": "string", "enum": [ "pav:createdBy" ] },
        { "type": "string", "enum": [ "pav:lastUpdatedOn" ] },
        { "type": "string", "enum": [ "oslc:modifiedBy" ] }
    ]
}
