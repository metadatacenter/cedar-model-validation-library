{
    "description": "Schema describing the schema of the '@context' field in the 'properties' field in a template TODO incomplete",
    "type": "object",
    "properties": {
        "type": { "type": "string", "enum": [ "string" ] },
        "properties": { "type": "object" },
        "patternProperties": { "type": "object" },
        "required": { "type": "array" },
        "additionalProperties": { "type": "boolean", "enum": [ false ] }
    },
    "required": [ "type", "properties", "patternProperties", "required", "additionalProperties" ],
    "additionalProperties": false
}
