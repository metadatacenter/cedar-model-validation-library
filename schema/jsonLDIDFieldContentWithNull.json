{
    "description": "Meta-schema that specifies the schema for a '@id' field that allows a null value",
    "type": "object",
    "properties": {
        "type": {
            "type": "array",  "minItems": 2,  "maxItems": 2,
            "items": [
                { "type": "string", "enum": [ "string" ] },
                { "type": "string", "enum": [ "null" ] }
            ]
        },
        "format": { "type": "string", "enum": [ "uri" ] }
    }
}